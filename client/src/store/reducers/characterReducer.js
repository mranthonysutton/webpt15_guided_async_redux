const initialState = {
  characters: [],
  isFetching: false,
  errors: "",
};

export const characterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
